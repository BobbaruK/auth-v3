import { LucideIconProps } from "@/types/icons";
import { ChevronDown } from "lucide-react";

export const ChevronDownIcon = ({ ...props }: LucideIconProps) => {
  return <ChevronDown {...props} />;
};

import { LucideIconProps } from "@/types/icons";
import { UserRoundCheck } from "lucide-react";

export const UnbanIcon = ({ ...props }: LucideIconProps) => {
  return <UserRoundCheck {...props} />;
};

import { ReactIconsProps } from "@/types/icons";
import { RiDiscordFill } from "react-icons/ri";

export const DiscordIcon = ({ ...props }: ReactIconsProps) => {
  return <RiDiscordFill {...props} strokeWidth={0} />;
};
